"""采集器层：五种采集器（RSS / GitHub API / arXiv API / 网页抓取 / HF Daily Papers）。

公共约束（设计纪要第 11、12 节）：
- 统一重试：3 次指数退避（5/15/45s），单次超时 30s，失败原因分类（超时/403/429/解析失败）
- 时效性分工：采集器只负责「抓最近的一窗」（API 排序 + max_items 限界），
  收录判定交给 DedupStore（没见过就收），水位线由 runner 在成功后推进——
  三者各管一段，晚索引的条目不会漏，失败后窗口自动补齐
- 输出统一为 NewsItem，source_url 从这里起随数据走，模型不生成链接
"""
